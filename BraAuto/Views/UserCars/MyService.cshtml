@model BraAuto.ViewModels.MyUserCarServiceModel

@{
    var loggedUser = (User)ViewBag.LoggedUser;
    var cloudinaryUrl = Configuration["Cloudinary:Url"] + Configuration["Cloudinary:AppName"] + "/";

    ViewBag.Title = loggedUser.IsService() ? "Service Cars" : "My Service Cars";
}

<style>
    .status-icon {
        color: #4971FF !important;
        font-size: 17px;
    }
</style>

<partial name="_BreadcrumbPartial" model="Model.ToBreadcrumb()" />
<partial name="_UserCarsTablePartial" />